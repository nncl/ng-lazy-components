import { VisibleWithDirective } from './visible-with.directive';

describe('VisibleWithDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleWithDirective();
    expect(directive).toBeTruthy();
  });
});
